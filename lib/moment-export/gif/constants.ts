/** GIF export width — matches PNG stage card (see STAGE_CARD_EXPORT_WIDTH). */
export const MOMENT_GIF_EXPORT_WIDTH = 1080
/** Catalog / audio-synced GIF — smaller file, audio carries rhythm. */
export const MOMENT_GIF_FPS = 12
/** External visual-loop GIF — match video FPS so fast effects (Pulse) sample cleanly. */
export const MOMENT_GIF_VISUAL_LOOP_FPS = 30
/** Supersample living-effect frames before GIF quantize (renderMomentFrame exportScale). */
export const MOMENT_GIF_VISUAL_LOOP_RENDER_SCALE = 2
